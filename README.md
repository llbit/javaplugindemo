Minimal Java Plugin System Demo
===============================

This is a demo of a very minimal plugin system for Java.

Usage:

    $ gradle jar
    $ java -cp host.jar:plugin.jar host.Application myplugin.Plugin
